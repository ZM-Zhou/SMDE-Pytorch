test_dataset:
  params:
    normalize_params: [0, 0, 0]
    is_fixK: v2

model:
  params:
    set_SCALE: 5.4 # for using the real baseline scale

_test: options/_base/datasets/kitti_test_384_improved.yaml
_base: options/PackNet/Packnet_M_192.yaml

